export const changeLayout = (layout) => ({
  type: 'CHANGE_LAYOUT',
  layout
})

export const changeTool = (tool) => ({
  type: 'CHANGE_TOOL',
  tool
})

export const filter = (filter) => ({
  type: 'FILTER',
  filter
})
